import { BlockButtonDirective } from './block-button.directive';

describe('BlockButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockButtonDirective();
    expect(directive).toBeTruthy();
  });
});
