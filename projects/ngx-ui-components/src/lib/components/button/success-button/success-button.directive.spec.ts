import { SuccessButtonDirective } from './success-button.directive';

describe('SuccessButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new SuccessButtonDirective();
    expect(directive).toBeTruthy();
  });
});
