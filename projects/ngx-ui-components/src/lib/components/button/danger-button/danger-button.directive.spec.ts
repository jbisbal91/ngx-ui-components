import { DangerButtonDirective } from './danger-button.directive';

describe('DangerButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new DangerButtonDirective();
    expect(directive).toBeTruthy();
  });
});
