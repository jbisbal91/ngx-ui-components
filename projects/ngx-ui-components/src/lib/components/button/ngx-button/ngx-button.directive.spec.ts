import { NgxButtonDirective } from './ngx-button.directive';

describe('NgxButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxButtonDirective();
    expect(directive).toBeTruthy();
  });
});
