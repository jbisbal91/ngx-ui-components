import { CardAvatarDirective } from './card-avatar.directive';

describe('CardAvatarDirective', () => {
  it('should create an instance', () => {
    const directive = new CardAvatarDirective();
    expect(directive).toBeTruthy();
  });
});
