import { SuffixDirective } from './suffix.directive';

describe('SuffixDirective', () => {
  it('should create an instance', () => {
    const directive = new SuffixDirective();
    expect(directive).toBeTruthy();
  });
});
