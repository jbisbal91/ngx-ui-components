import { ColumnGroupDirective } from './column-group.directive';

describe('ColumnGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new ColumnGroupDirective();
    expect(directive).toBeTruthy();
  });
});
